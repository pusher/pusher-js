#!/bin/bash

if [ "$TRAVIS_BRANCH" == "master" ]; then
  export CI='full' ;
else
  export CI='basic' ;
fi

make web_unit && \
make web_integration && \
make node_unit && \
make node_integration && \
make worker_unit && \
make worker_integration
