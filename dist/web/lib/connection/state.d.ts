declare enum ConnectionState {
    OPEN,
    CLOSED,
    NEW,
    INITIALIZED,
    INITIALIZING,
    CONNECTING,
    FAILED,
    DISCONNECTED,
    UNAVAILABLE,
    CONNECTED,
}
export default ConnectionState;
