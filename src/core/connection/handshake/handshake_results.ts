enum HandshakeResults {
  CONNECTED = <any>"connected",
  BACKOFF = <any> "backoff",
  SSL_ONLY = <any> "ssl_only",
  REFUSED = <any> "refused",
  RETRY = <any> "retry",
  ERROR = <any> "error"
}

export default HandshakeResults;
